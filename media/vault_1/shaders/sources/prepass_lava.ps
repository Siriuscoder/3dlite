uniform sampler2D diffuse;
uniform float animcounter; // [0..1]

in vec2 iuv;
in vec3 ivv;
in vec3 wnorm;

void main()
{
    // sampling diffuse color 
    gl_FragColor = vec4(texture2D(diffuse, vec2(iuv.x, iuv.y + animcounter)).rgb, 1.0);
}